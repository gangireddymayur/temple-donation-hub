# MariaDB (from Plesk → Connection info)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=mayur_
DB_USER=mayur_app
DB_PASSWORD=PASTE_PASSWORD_HERE

# JWT — generate a long random string (e.g. openssl rand -hex 64)
JWT_SECRET=replace-with-long-random-string

# Server
PORT=8080
CORS_ORIGIN=*
